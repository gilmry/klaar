# `Connection` doit valoir `upgrade` pour une socket, et rien pour le reste :
# le poser en dur casserait le maintien de connexion des requêtes ordinaires.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
